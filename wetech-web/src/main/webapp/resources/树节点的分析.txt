添加--->
  1、对于添加的子节点而言：不用考虑
  2、对于父节点而言：如果添加之前节点被选中就不用添加父节点
                     如果添加之前节点没有被选中，就需要添加所有的父节点(beforeCheck)
删除--->
  1、对于子节点的删除：所有被选中的子节点都要删除(beforeCheck)
  2、对于父节点而言:如果取消之后父节点依然存在就不操作，父节点如果不存在就进行删除(onCheck)
